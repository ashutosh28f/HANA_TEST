name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: |
          echo "hello world!"
          echo " First messages from git hub actions"
      - name: print goodbye
        run: |
          echo "Goodbye!"
          echo " and take care!!!"
